import styled from "styled-components";

const Form = styled.form`
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  width: 20rem;
  button {
    margin-top: 1rem;
  }
`;

export default { Form };
